{
	"name": "dead-spin",
	"private": true,
	"version": "0.0.0",
	"description": "Dead Spin — Telegram Mini App game (monorepo).",
	"packageManager": "pnpm@9.12.0",
	"engines": {
		"node": ">=22.0.0"
	},
	"scripts": {
		"typecheck": "pnpm -r run typecheck",
		"test": "pnpm -r run test",
		"validate:levels": "pnpm --filter @dead-spin/levels run validate"
	},
	"devDependencies": {
		"typescript": "^5.6.3",
		"tsx": "^4.19.1",
		"@types/node": "^22.7.5"
	}
}
