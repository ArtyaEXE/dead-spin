/** Бренд-книга: переписана под текущее состояние проекта. */
import {mkdirSync, writeFileSync} from 'node:fs';
import {dirname, join} from 'node:path';
import {fileURLToPath} from 'node:url';

const OUT = process.env['DS_OUT'] ?? join(dirname(fileURLToPath(import.meta.url)), '..', 'out', 'project');

mkdirSync(OUT, {recursive: true});

writeFileSync(
	join(OUT, 'README.md'),
	`# Dead Spin — «Жесть и копоть»

Мобильная аркада: ракета на тяге летит по пещере астероида, собирает звёзды
и садится на площадку. Управление в один палец, забег на пару минут.

Это система рисованной от руки жестяной игрушки. Школа Amanita Design
скрещённая с ретрофутурной ракетой 1950-х. Look редкий для мобильного рынка,
опознаётся по одному скриншоту и потому работает как маркетинговый актив,
а не только как вкусовщина.

Чего в этом мире нет: фотореализма, аэрографа, неона, свечений ради свечений,
объёмного 3D-рендера, частиц «для красоты».

## Откуда система взялась

Игра была наполовину сгенерирована нейросетью, и это было видно. Рядом жили
две техники: честная рука с перовым контуром и фотореалистичные текстуры без
линии — ледяные пещеры, красно-зелёный сток-камень, аэрографная мина. Второй
слой и читался как слоп: у него нет линии, нет авторского решения и нет общей
светотени с остальной игрой.

Плюс три измеримые поломки: корабль был темнее декора, в CSS лежало двадцать
чистых чёрных, двенадцать кеглей и одиннадцать радиусов, а визуал не
поддерживал шутку, которую уже произносит логотип.

## Три закона

**Первый: одна техника.** Каждый ассет мира имеет видимый перовой контур и
плоскую или двухтоновую заливку. Ассет без контура в игру не попадает. Если
объект нельзя нарисовать пером — это не объект этого мира, а текстура из
стока. Поправка для иконок HUD описана ниже и в разделе «Иконки HUD».

**Второй: три слоя яркости.** Каждый пиксель принадлежит ровно одному слою, и
слой диктует его светлоту. Корабль — самое светлое пятно кадра, всегда.
Насыщенный оранжевый есть только у того, что тебя убьёт. Мир тише героя
минимум на 45% светлоты. Это не стилистика, а бюджет внимания: в рефлекторной
игре у игрока есть сто миллисекунд, чтобы разобрать кадр на «я / цель / смерть».

**Третий: токены, не значения.** Шесть кеглей, три радиуса, три кривые. Чистый
чёрный запрещён: тень на тёплой поверхности обязана быть тёплой.

## Цвет

Роли, а не набор свотчей. Ни один компонент не пишет хекс напрямую.

- \`void\` и семейство \`rock-*\` — слой мира. Рецессивный, тихий, никогда не
  светлее \`rock-500\`.
- \`hull\`, \`rim\` — герой. \`rim\` не расходуется на декор: это метка «это ты».
- \`goal\` — цель. Единственное золото на экране.
- \`danger\` — смерть. Нигде, кроме угрозы: ни в интерфейсе, ни в декоре, ни
  на факеле двигателя.
- \`paper\`, \`ink\` — интерфейс. Крашеная жесть и бумажная наклейка, не резной
  камень.

Вторичный текст подтонирован от поверхности, никогда не нейтрально-серый.
Один акцент на экран: \`goal\` и \`danger\` не встречаются в одном элементе.

## Типографика

Две гарнитуры. **Road Rage** — голос бренда: логотип, заголовки, крупные
числа. Только от 24 px: мельче она нечитаема, а стояла она на 11–15 px.
**Rubik** — интерфейс и цифры, обязательно с табличными цифрами: без них
таймер дёргается на каждом кадре.

Шесть кеглей. Два задокументированных исключения: дисплейный текст крупнее
24 px может задаваться через \`clamp()\`, а \`font-size\` внутри круглой кнопки
задаёт размер глифа, а не текста.

## Свет

В тёмной пещере объект без собственного света висит в пустоте. Свет делает
три вещи сразу: показывает, где герой, показывает, куда лететь, и превращает
провал в пространство. Это и есть разница между «правильно» и «приятно».

Светятся корабль, финиш, звёзды и нагревающаяся мина. Режим аддитивный.
Радиальный градиент здесь не нарушает первый закон: запрет на градиенты
относится к объектам, а свет объектом не является. Подробности — в разделе
«Свет».

## Иконки интерфейса

Здесь у системы две разные вещи, и путать их нельзя.

**Глифы** (\`src/ui/Icon.tsx\`) означают действие — закрыть, играть,
приблизить. Геометрия на сетке 24, один штрих, цвет \`currentColor\`.

**Иконки HUD** означают предмет — топливо, монеты, мина. Сплошные силуэты с
[game-icons.net](https://game-icons.net) под CC BY 3.0, перекрашенные по роли.

На иконки HUD первый закон **не распространяется**, и это поправка, а не
обход: на 24 пикселях второй тон не виден, а лишняя обводка съедает форму.
Двухтоновый закон зарабатывает своё место на объектах мира, где размер
большой и важен материал.

## Тон

Игра шутит: на логотипе написано THRUST ME, I'M A PILOT. Долгое время визуал
шутку не поддерживал и выглядел как мрачный стимпанк 2012 года. Направление —
не мрачный космос, а неудачливый механик в консервной банке.

Копирайт конкретный и сухой, шутка в ситуации, а не в прилагательных. Кнопка
называет действие, ошибка называет проблему и выход из неё.

## Что проверяется машиной

\`pnpm art:check\` меряет каждый ассет мира: светлота по CIELAB, 95-й процентиль
по непрозрачным пикселям, доля тона угрозы. Роняет CI. Правило «корабль —
самое светлое пятно» невозможно соблюдать на глаз: ровно так в проекте и
появился фон ярче героя.

Иконки HUD через эту проверку не проходят: замер по слоям про то, что борется
за внимание в игровом кадре, а иконки живут на плашках интерфейса.

## Вектор, не растр

**В проекте не осталось ни одного растрового файла.** 120 SVG, 295 КБ; кроме
них в \`public/\` только шрифты, музыка и страница политики.

Весь игровой слой рисуется генераторами: порода пяти миров, угрозы, цель,
корабль со скинами, кадры взрыва, декор, фоны, кадры комикса, фавикон. То,
что рисует программа с перовым контуром и плоской заливкой, физически не
может оказаться фотореалистичной текстурой из стока — первый закон
соблюдается по построению, а не на честном слове.

Логотип не файл, а компонент: буквы рисует живой Road Rage, загруженный
страницей. Правка надписи это правка строки.

Плашки интерфейса не растеризуются вообще: семь подложек заменены двумя
материалами в CSS.

## Декор снят

183 предмета убраны со всех тридцати уровней. В тёмной пещере со светом
каждый лишний силуэт спорит с миной и звездой за те сто миллисекунд, за
которые игрок разбирает кадр. Ассеты остались — см. раздел «Декор».

## Лицензии

Иконки HUD — game-icons.net, CC BY 3.0. Авторы: Lorc, Delapouite, Skoll,
Cathelineau, guard13007 и участники набора badges. Исходники вендорены в
репозиторий вместе с текстом лицензии; атрибуция выведена в экран настроек
игры, потому что увидеть её должен игрок, а не разработчик.

Шрифт Rubik — SIL Open Font License, self-hosted. Road Rage — шрифт,
унаследованный от первой версии проекта.
`,
	'utf8',
);

console.log('бренд-книга переписана');
